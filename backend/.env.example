# AgriNLP Backend Environment Variables

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
# IMPORTANT: Add at least $5 to your OpenAI account before generating a key
OPENAI_API_KEY=your_openai_api_key_here

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration (Optional - for future use)
# MONGODB_URI=mongodb://localhost:27017/agrinlp

# Cache Configuration
CACHE_TTL=3600

# CORS Configuration (Optional - defaults to all origins)
# ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-url.com

# Rate Limiting (Optional - for future use)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100
